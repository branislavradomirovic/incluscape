[server]
headless = true
maxUploadSize = 20   # MB — enforced by Streamlit's file uploader widget
fileWatcherType = "none"
runOnSave = false
port = 8501

[theme]
base = "light"

[logger]
level = "info"

[browser]
gatherUsageStats = false
